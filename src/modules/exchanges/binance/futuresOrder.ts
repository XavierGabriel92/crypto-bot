import { Binance } from "binance-api-node";

function futuresOrder(client: Binance) {
  // const order = await client.futuresOrder({
  //   symbol: "BTCUSDT",
  //   side: "BUY",
  //   type: "MARKET",
  //   quantity: "1",
  // });
  // console.log(JSON.stringify(order));
}

export default futuresOrder;
